# Wolf Small Report - Project Handover Status
**Date**: 2025-12-06
**Status**: Ready for Phase 2 Implementation

## 1. Project Overview
This project aims to automate the scoring of essays shared in a LINE group ("Wolf Small Report").
- **Goal**: Ingest links -> Download content -> Run 4-stage LLM evaluation -> Generate Reports.
- **Current Progress**: Phase 1 (Data Prep) is complete. Phase 2 (Pipeline Implementation) is designed but not started.

## 2. Existing Artifacts
- **`specs/SDD_PLAN_PHASE2.md`**: Official development plan for Phase 2 & 3. **Follow this strictly.**
- **`wolf_colab_small_report.py`**: Legacy Colab prototype.
  - **Status**: Reference Only.
  - **Contains**: Logic for parsing LINE txt logs, classifying domains, and basic Google Docs/Drive downloading.
  - **Action**: Adapt its regex (`extract_gdoc_id`, `extract_drive_file_id`) and download logic (`simple_download`) into the new `DownloadAgent`.
- **`src/core/db.py`**: Database schema (Ready).
- **`data/output/links_with_type.csv`**: Input data for Phase 2.

## 3. Next Steps (for the next AI Assistant)
Please start **Phase 2 Implementation** immediately following `specs/SDD_PLAN_PHASE2.md`.

### Immediate Tasks
1.  **Orchestrator (`src/orchestrator.py`)**:
    - Implement `ingest_csv` to load `data/output/links_with_type.csv` into SQLite.
    - Note: The CSV was generated by the Colab script. Trust its columns.
2.  **Download Agent (`src/agents/download_agent.py`)**:
    - **CRITICAL**: Port the logic from `wolf_colab_small_report.py` (lines 316-372) into this class.
    - Use the logic for `gdocs` (/export?format=txt) and `gdrive_file` handling.
    - Ensure it runs locally on Linux (not Colab).

## 4. Environment
- **OS**: Linux
- **Python Path**: `/home/sp/SP_WORK/wolf_small_report`
- **DB**: SQLite (`wolf_small_report.db`)

## 5. Notes
- The user prefers **Traditional Chinese** for all communication and documentation.
- Do not modify `wolf_colab_small_report.py`. It is a static archive of the previous work.
